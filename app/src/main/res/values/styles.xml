<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolbarTheme">
        <!-- Color used for the title of the Toolbar - as long as not overridden -->
        <item name="android:textColorPrimary">@color/colorDarkGrey</item>
        <!-- Used to color back button and three dots -->
        <item name="android:textColorSecondary">@color/colorDarkGrey</item>
    </style>

    <style name="Movie.Details.Label" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="Movie.Details.Content" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/colorDarkGrey</item>
        <item name="android:textSize">@dimen/text_normal</item>
    </style>

</resources>
